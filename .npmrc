# Hoisting off by default in pnpm, which is the point: a package that is not a declared
# dependency must not be importable. Phantom dependencies are how a build works locally and
# fails in CI.
strict-peer-dependencies=false
